# Installation Instructions:
 - Run "pip install pipenv" if "pipenv" not installed
 - Enter virtual environment by running "pipenv shell"

# Running instructions:
 - Run "scrapy crawl articles -o '../../../Data & Preprocessing Scripts/diario_noticias_50iter.json'" to run the spider and save results in correct directory

# Notes:
 - Scraper currently limited by number of times it can load more content on a given page