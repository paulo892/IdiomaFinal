import React from 'react';

// a callback constant for use when Auth0 authentication is loading
const Callback = () => (
  <div className="container">
    <h4>Loading...</h4>
  </div>
);

export default Callback;